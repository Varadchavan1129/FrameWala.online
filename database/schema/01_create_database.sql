-- 01_create_database.sql
-- Purpose: Create the database for FrameWala and select it.

-- Create the database if it doesn't already exist
CREATE DATABASE IF NOT EXISTS framewala_db;

-- Select the database for subsequent table creation scripts
USE framewala_db;
