# FrameWala — Test Credentials

Frontend-only build (no backend). Auth is a local mock:
- Login: any email + any password works (creates a local session in browser localStorage).
- Register: any details create a local account.

Cart, wishlist, and session persist in browser localStorage (survive refresh).

Coupons (checkout): FRAMEWALA10 (10% off), FRAMEWALA15 (15% off).
